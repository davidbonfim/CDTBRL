# Assets (GOLD ASA)

Este diretório contém os arquivos off-chain usados como referência pelo ASA `GOLD` (ex.: metadados ARC-3 e imagem).

- Metadados ARC-3: `assets/arc3/duciello-group-gold.json`
- Imagem (a ser adicionada): `assets/images/duciello-group-gold.png`

Importante: se o ASA for criado com `metadataHash`, qualquer alteração no arquivo de metadados muda o hash e quebra a validação ARC-3. Evite reformatar ou editar `assets/arc3/duciello-group-gold.json` depois da emissão.

